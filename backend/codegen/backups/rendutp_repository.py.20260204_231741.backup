"""
Repository rendutp : accès aux données (pattern Repository).
"""
from app.core.repositories.base import BaseRepository
from app.espace_student.models import RenduTP


class RenduTPRepository(BaseRepository):
    model = RenduTP

    def get_queryset(self):
        return super().get_queryset()